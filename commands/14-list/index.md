---
layout: default
parent: Commands
title: list
nav_order: 14
has_children: false
permalink: /list
has_toc: false
---

# list

The `list` command lists the elements within a workspace.

## Options

- `-workspace`: The path or URL to the workspace JSON/DSL file (required)

## Example

To list the elements in a JSON workspace definition:

```
list -workspace workspace.json
```